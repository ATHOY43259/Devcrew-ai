# Tested on Python 3.11 with the versions below (Aug 2026).
langgraph>=1.2
langchain>=1.3
langchain-openai>=1.5
# Optional second LLM provider (set LLM_PROVIDER=gemini in .env to use it —
# Google AI Studio has a free tier, no billing setup required):
langchain-google-genai>=2.0
streamlit>=1.61
pandas>=2.0
python-dotenv>=1.0
pytest>=8.0

# Member 1 (persistent checkpoints across app restarts):
langgraph-checkpoint-sqlite>=2.0

# Member 2 (RAG / knowledge base — uses Chroma's built-in local embedding,
# no API key required):
chromadb>=0.5

# Member 2 (web search tool, no API key required):
duckduckgo-search>=6.0
